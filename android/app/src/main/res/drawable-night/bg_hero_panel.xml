<?xml version="1.0" encoding="utf-8"?>
<!-- Dark mode hero panel: deep teal-to-dark gradient matching PC aesthetics -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:type="linear"
        android:angle="135"
        android:startColor="@color/heroStart"
        android:centerColor="@color/surfaceStrong"
        android:endColor="@color/heroEnd" />
    <corners android:radius="20dp" />
    <stroke
        android:width="1dp"
        android:color="@color/heroBorder" />
</shape>
